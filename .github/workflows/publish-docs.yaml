name: Publish Docs

on:
  push:
    branches:
      - main

permissions:
  actions: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  publish-docs:
    name: Publish Docs
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v7

    - name: Dotnet Setup
      uses: actions/setup-dotnet@v5
      with:
        global-json-file: global.json

    - run: dotnet build -c Release

    - run: dotnet run -c Release --project src/KubernetesCRDModelGen.Sync/KubernetesCRDModelGen.Sync.csproj -- --contentRoot $PWD --DocFXGenerator true

    - run: dotnet tool update -g docfx
    - run: docfx docfx.json

    - name: Upload artifact
      uses: actions/upload-pages-artifact@v5
      with:
        path: '_site'

    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v5